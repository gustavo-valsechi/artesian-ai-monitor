"use client"

import styled from "styled-components"

export const Container = styled.div`
    padding: 0 2rem 2rem;
    display: flex;
    flex-direction: column;
    gap: 2rem;

    .monitor-content {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }
`;